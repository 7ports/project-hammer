# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files — never commit
.env
.env.*
!.env.example
project-hammer-important-values.txt

# Claude Code local settings
.claude/settings.local.json

# Terraform local state (lock file is committed, provider cache is not)
infra/.terraform/

# Voltron dashboard output
.voltron/

# Fly.io deploy env (credentials — never commit)
.fly-deploy-env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
